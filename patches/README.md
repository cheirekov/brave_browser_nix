# Downstream patches

There are currently no downstream source patches. Tor is disabled with Brave's
supported `enable_tor=false` GN argument, and the separate profile is selected
by the packaging wrapper. Brave's own patch series is part of the pinned
`brave-core` source and is applied unchanged.
